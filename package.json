{
  "name": "phone-as-webcam",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp _redirects dist",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.3.0"
  }
}
